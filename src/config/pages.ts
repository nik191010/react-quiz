interface Pages {
  name: string;
  link: string;
}

const pages: Pages[] = [
  { name: 'Home', link: '/' },
  { name: 'Create Quiz', link: '/create' },
  { name: 'Play Quiz', link: '/play' },
];

export default pages;
